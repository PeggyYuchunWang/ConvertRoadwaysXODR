 
    # _revMajor = attr.ib(default=int(0))
    # _revMinor = attr.ib(default=int(0))
    # _name = attr.ib(default="")
    # _version = attr.ib(default="")
    # _date = attr.ib(default="")
    # _north = attr.ib(default=float(0))
    # _south = attr.ib(default=float(0))
    # _east = attr.ib(default=float(0))
    # _west = attr.ib(default=float(0))
    # _vendor = attr.ib(default="")

                # framework.header = Header(attrib=
            #     {"revMajor" : int(att["revMajor"]),
            #     "revMinor" : int(att["revMinor"]),
            #     "name" : att["name"],
            #     "version" : att["version"],
            #     "date" : att["date"],
            #     "north" : float(att["north"]),
            #     "south" : float(att["south"]),
            #     "east" : float(att["east"]),
            #     "west" : float(att["west"]),
            #     "vendor" : ""}
            # )
    
    # attrib = attr.ib(default={
    #     "revMajor" : _revMajor,
    #     "revMinor" : _revMinor,
    #     "name" : _name,
    #     "version" : _version,
    #     "date" : _date,
    #     "north" : _north,
    #     "south" : _south,
    #     "east" : _east,
    #     "west" : _west,
    #     "vendor" : _vendor 
    #     })

    # geoReference = attr.ib(type=Geo_Reference, default=None)
    # offset = attr.ib(type=Offset, default=None)